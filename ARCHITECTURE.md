# 📐 系统架构图

## 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户浏览器                                  │
│                     (React Application)                         │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ├─────────────────────────────────────────────┐
                 │                                             │
                 ▼                                             ▼
    ┌────────────────────┐                        ┌────────────────────┐
    │   React Components  │                        │   Contexts (State) │
    │  - AddBook.tsx     │◄──────────────────────►│  - BooksContext    │
    │  - Inventory.tsx   │                        │  - UsersContext    │
    │  - BookDetail.tsx  │                        └─────────┬──────────┘
    │  - Users.tsx       │                                  │
    └──────────┬─────────┘                                  │
               │                                            │
               ▼                                            ▼
    ┌────────────────────────────────────────────────────────────┐
    │               Data Service Layer                            │
    │            (services/dataService.ts)                        │
    │  ┌──────────────┬──────────────┬──────────────────────┐   │
    │  │ Books API    │ Users API    │ Borrow Records API   │   │
    │  │ - getAll     │ - getAll     │ - getAll             │   │
    │  │ - getById    │ - getById    │ - getByBookId        │   │
    │  │ - add        │ - add        │ - create             │   │
    │  │ - update     │ - update     │ - return             │   │
    │  │ - delete     │ - delete     │                      │   │
    │  │ - search     │              │                      │   │
    │  └──────────────┴──────────────┴──────────────────────┘   │
    └─────────────────────────┬──────────────────────────────────┘
                              │
                              ▼
                   ┌────────────────────┐
                   │  Supabase Client   │
                   │ (lib/supabase.ts)  │
                   └─────────┬──────────┘
                             │
                             │ HTTPS/WebSocket
                             │
                             ▼
         ┌───────────────────────────────────────┐
         │          Supabase Cloud               │
         │  ┌─────────────────────────────────┐  │
         │  │      PostgreSQL Database        │  │
         │  │  ┌──────────┬──────────────┐   │  │
         │  │  │  books   │ borrow_recs  │   │  │
         │  │  ├──────────┼──────────────┤   │  │
         │  │  │  users   │  (triggers)  │   │  │
         │  │  └──────────┴──────────────┘   │  │
         │  └─────────────────────────────────┘  │
         │                                       │
         │  ┌─────────────────────────────────┐  │
         │  │     Row Level Security (RLS)    │  │
         │  └─────────────────────────────────┘  │
         │                                       │
         │  ┌─────────────────────────────────┐  │
         │  │       Realtime (Optional)       │  │
         │  └─────────────────────────────────┘  │
         └───────────────────────────────────────┘
```

## 数据流图

### 1. 页面加载 - 读取数据

```
用户访问页面
     │
     ▼
┌─────────────┐
│ Inventory   │
│   Page      │
└──────┬──────┘
       │ useBooks()
       ▼
┌─────────────┐
│ BooksContext│──► useEffect(loadBooks)
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ dataService.ts  │──► getAllBooks()
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Supabase Client │──► .from('books').select('*')
└──────┬──────────┘
       │
       ▼ HTTPS Request
┌─────────────────┐
│ Supabase Cloud  │
│  PostgreSQL DB  │
└──────┬──────────┘
       │
       ▼ Response (JSON)
返回图书数据
       │
       ▼
Context 更新 state
       │
       ▼
页面重新渲染
       │
       ▼
显示图书列表 ✅
```

### 2. 添加图书 - 写入数据

```
用户填写表单
     │
     ▼
点击"保存并添加"
     │
     ▼
┌─────────────┐
│ AddBook.tsx │──► handleSubmit()
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ BooksContext│──► addBook(formData)
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ dataService.ts  │──► addBook(book)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Supabase Client │──► .from('books').insert(data)
└──────┬──────────┘
       │
       ▼ HTTPS POST
┌─────────────────┐
│ Supabase Cloud  │
│  PostgreSQL DB  │──► INSERT INTO books ...
└──────┬──────────┘
       │
       ▼ 触发器执行
    (自动更新时间戳)
       │
       ▼ Response
返回新建图书数据
       │
       ▼
Context 更新 books 数组
       │
       ▼
导航到 /inventory
       │
       ▼
显示新添加的图书 ✅
```

### 3. 搜索图书 - 查询数据

```
用户输入搜索词
     │
     ▼
onChange 事件
     │
     ▼
┌─────────────┐
│ Inventory   │──► setSearch(value)
└──────┬──────┘
       │
       ▼
计算 filteredBooks
       │ (前端过滤)
       ▼
显示过滤结果 ✅

或者 (如需服务端搜索)
     │
     ▼
┌─────────────────┐
│ dataService.ts  │──► searchBooks(query)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Supabase Client │──► .or(title.ilike.%query%)
└──────┬──────────┘
       │
       ▼ HTTPS GET
┌─────────────────┐
│ Supabase Cloud  │──► WHERE title ILIKE '%query%'
└──────┬──────────┘
       │
       ▼
返回匹配结果
```

## 数据库结构关系图

```
┌──────────────────────┐
│       books          │
│  ──────────────────  │
│  id (PK)             │◄────────┐
│  title               │         │
│  author              │         │
│  isbn (UNIQUE)       │         │
│  status              │         │
│  category            │         │
│  ...                 │         │
└──────────────────────┘         │ FK: book_id
                                 │
                                 │
                   ┌──────────────────────────┐
                   │    borrow_records        │
                   │  ──────────────────────  │
                   │  id (PK)                 │
                   │  book_id (FK)            │◄───┐
                   │  user_id (FK)            │───┐│
                   │  borrow_date             │   ││
                   │  due_date                │   ││
                   │  return_date             │   ││
                   │  status                  │   ││
                   └──────────────────────────┘   ││
                                                  ││
                                                  ││
┌──────────────────────┐                          ││
│       users          │                          ││
│  ──────────────────  │                          ││
│  id (PK)             │◄─────────────────────────┘│
│  name                │                           │
│  email (UNIQUE)      │                           │
│  phone               │                           │
│  active_borrow_count │◄──────────────────────────┘
│  status              │     (自动更新via触发器)
│  ...                 │
└──────────────────────┘
```

## 触发器逻辑

### 1. 更新时间戳触发器
```
ON UPDATE books/users/borrow_records
  ▼
SET updated_at = NOW()
```

### 2. 图书状态更新触发器
```
ON UPDATE borrow_records
  │
  ├─ 如果 status 变为 'returned'
  │   ▼
  │  UPDATE books SET status = 'in-library'
  │
  └─ 如果 status 变为 'active'
      ▼
     UPDATE books SET status = 'lent-out'
```

### 3. 借阅计数触发器
```
ON INSERT borrow_records (status='active')
  ▼
UPDATE users SET active_borrow_count += 1

ON UPDATE borrow_records (active → returned)
  ▼
UPDATE users SET active_borrow_count -= 1
```

## 安全层 (RLS)

```
       ┌────────────┐
       │  Browser   │
       └──────┬─────┘
              │ Request + anon_key
              ▼
       ┌────────────┐
       │ Supabase   │
       │   API      │
       └──────┬─────┘
              │
              ▼
       ┌────────────────┐
       │  RLS Policies  │◄── 开发环境: 允许所有操作
       └──────┬─────────┘    生产环境: 需要认证
              │
              ▼ 验证通过
       ┌────────────────┐
       │   PostgreSQL   │
       │    Database    │
       └────────────────┘
```

## 组件层次结构

```
App.tsx
 ├─ BooksProvider (Context)
 │   │
 │   └─ AppContent
 │       ├─ Sidebar
 │       ├─ Header
 │       └─ Routes
 │           ├─ Dashboard
 │           ├─ Inventory ──► 使用 useBooks()
 │           ├─ AddBook ──────► 使用 useBooks()
 │           ├─ BookDetail ───► 使用 useBooks()
 │           ├─ Users
 │           ├─ Reports
 │           └─ Settings
 │
 └─ (未来可扩展)
     ├─ UsersProvider
     ├─ BorrowProvider
     └─ AuthProvider
```

## 技术栈总览

```
┌──────────────────────────────────────┐
│          Frontend (Browser)          │
│  ┌────────────────────────────────┐  │
│  │  React 19 + TypeScript         │  │
│  │  - React Router (路由)         │  │
│  │  - Context API (状态管理)     │  │
│  │  - Tailwind CSS (样式)        │  │
│  └────────────────────────────────┘  │
└────────────┬─────────────────────────┘
             │
             │ @supabase/supabase-js
             │
┌────────────▼─────────────────────────┐
│       Backend (Supabase Cloud)       │
│  ┌────────────────────────────────┐  │
│  │  PostgreSQL 15                 │  │
│  │  - Row Level Security          │  │
│  │  - Triggers & Functions        │  │
│  │  - Realtime Subscriptions      │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │  RESTful API (PostgREST)       │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │  Storage (可选，存储图片)      │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

## 部署架构 (生产环境)

```
         ┌────────────┐
         │   Users    │
         └──────┬─────┘
                │ HTTPS
                ▼
         ┌────────────┐
         │  Vercel/   │
         │  Netlify   │──► React 应用 (静态文件)
         └──────┬─────┘
                │ API Calls
                ▼
         ┌────────────┐
         │  Supabase  │
         │   Cloud    │──► 数据库+API
         └────────────┘
```

---

**说明**:
- `──►`: 数据流向
- `◄──`: 数据返回
- `(FK)`: 外键
- `(PK)`: 主键
- `✅`: 操作成功完成
